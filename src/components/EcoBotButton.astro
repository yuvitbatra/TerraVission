---
// EcoBotButton.astro — mounts the EcoBot React island (floating button + chat
// panel) on every page via BaseLayout. The island manages its own open/close.
import EcoBot from "@/components/islands/EcoBot.tsx";
---

<EcoBot client:idle />
